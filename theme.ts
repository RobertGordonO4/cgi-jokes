import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "#76c7ff", // chuck-blue
    },
    secondary: {
      main: "#8b5a2b", // chuck-brown
    },
    warning: {
      main: "#f15b23", // chuck-orange
    },
  },
})

export default theme